import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();

  // Primary
  static const Color primary = Color(0xFF6C5DD3); // Purple
  static const Color onPrimary = Colors.white;

  // Backgrounds
  static const Color background = Color(0xFF111114); // Very dark
  static const Color surface = Color(0xFF1F1F24); // Panel bg
  static const Color surfaceHighlight = Color(0xFF2B2B33); // Hover/Active

  // Text
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFFB3B3B9);
  static const Color textDisabled = Color(0xFF5A5A60);

  // Semantic
  static const Color success = Color(0xFF28C940);
  static const Color error = Color(0xFFFF5F57);
  static const Color warning = Color(0xFFFFBD2E);
  static const Color info = Color(0xFF00BFFF);

  // Border
  static const Color border = Color(0xFF2D2D35);
  static const Color borderSubtle = Color(0xFF232328);
}
